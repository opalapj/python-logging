[project]
name = "python-logging"
version = "0.1.0"
authors = [
    {name = "Piotr Opała", email = "opalapj@gmail.com"},
]
license = {text = "MIT"}
dependencies = ["pyyaml>=6.0.3"]
requires-python = "==3.13.*"

[tool.pdm]
distribution = false
