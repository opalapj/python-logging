version: 1
disable_existing_loggers: false
root:
  handlers:
    - default
handlers:
  default:
    class: logging.StreamHandler
    formatter: default
formatters:
  default:
    format: "%(levelname)s:%(name)s:%(message)s"
